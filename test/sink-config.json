{
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": 1,
    "connection.url": "jdbc:postgresql://postgres/sink_test?user=postgres",
    "topics": "sink-test",
    "insert.mode": "insert",
    "table.name.format": "sink_test",
    "auto.create": true
}
